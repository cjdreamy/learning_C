#include <stdlib.h>
#include <stdio.h>
#include <time.h>

int main(){
    //  srand(time(NULL));
    // int num =  rand();
    // double time_Elapsed = time(NULL);
    // printf("\nrandom num: %d , timeelapsed: %d", num, time_Elapsed);


int x,y,z, max;
 
printf("enter 3 nums");
scanf(" %d %d %d", &x, &y, &z);
 x > y ? printf("%d", x) : printf ("%d ", y) ;
  
printf("%d ", max); 

    }